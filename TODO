Merge MagicFuture back into tasklet.Future class.

Explain at the top of the tutorial why async APIs are good.

Go more public.

Fix up code: style, docstrings, TODOs.  Increase coverage again.

Separate tutorial, user guide, reference manual.  Explain the things
that Brett & Nick asked in feedback.

Add Query methods going through default context for map(callback),
map_async(callback), fetch(limit), fetch_async(limit), count(limit),
count_async(limit).

Add a public API for QueryOptions.

Handle exceptions in callbacks uniformly.

Ensure that Models are pickled as pbs, so caching doesn't have
to do anything special.
